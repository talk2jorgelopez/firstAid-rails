<%= form_for @vital, :html => { :class => "form-horizontal vital" } do |f| %>

  <% if @vital.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@vital.errors.count, "error") %> prohibited this vital from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @vital.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :vital_type, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :vital_type, :class => 'form-control' %>
    </div>
    <%=f.error_span(:vital_type) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                vitals_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
