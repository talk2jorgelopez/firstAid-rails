<%= form_for @severity, :html => { :class => "form-horizontal severity" } do |f| %>

  <% if @severity.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@severity.errors.count, "error") %> prohibited this severity from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @severity.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :severity_level, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :severity_level, :class => 'form-control' %>
    </div>
    <%=f.error_span(:severity_level) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                severities_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
